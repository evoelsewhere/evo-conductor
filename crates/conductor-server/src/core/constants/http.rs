//! Transport-level values.

/// Prefix every API route is nested under.
pub const API_PREFIX: &str = "/api";

/// The console entry point served for any unmatched path.
pub const SPA_INDEX_FILE: &str = "index.html";
